<%= form_for(page) do |f| %>
  <% if page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.cktext_area :content %>
  </div>

 <div class="form-group">
    <%= f.label :img, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.file_field :img, :class => 'form-control' %>
    </div>
    <%=f.error_span(:img) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
